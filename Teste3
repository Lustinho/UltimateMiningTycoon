-- =============================================
-- UMT - Interface Simples Mobile (Sem Library Externa)
-- Botão circular arrastável + Menu Farming
-- =============================================

local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local Workspace = game:GetService("Workspace")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")

local player = Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()

-- EspLibrary (sistema de ESP completo)
local EspLibrary, EspLibraryFunctions = loadstring(game:HttpGet("https://gist.githubusercontent.com/VertigoCool99/2bcff189f55663147f8d63cb5b2012d9/raw/c15541927610e44cfc31d640a1ddce50cda9e680/EspLibrary.lua"))()

-- ==================== CONFIGURAÇÕES ====================
local Settings = {
    AutoMine = false,
    AutoSell = false,
    MiningRange = 150,
    OreHitbox = false,
    HitboxSize = 8,
}

local Tool = nil

local function GetTool()
    for _, v in pairs(character:GetChildren()) do
        if v:FindFirstChild("EquipRemote") or string.find(v.Name:lower(), "pickaxe") then
            return v
        end
    end
    return nil
end

-- ==================== BOTÃO CIRCULAR ARRASTÁVEL ====================
local screenGui = Instance.new("ScreenGui")
screenGui.ResetOnSpawn = false
screenGui.Parent = player:WaitForChild("PlayerGui")

local button = Instance.new("TextButton")
button.Size = UDim2.new(0, 70, 0, 70)
button.Position = UDim2.new(1, -90, 1, -90)
button.BackgroundColor3 = Color3.fromRGB(220, 0, 0)
button.Text = "OFF"
button.TextColor3 = Color3.new(1, 1, 1)
button.TextScaled = true
button.Font = Enum.Font.GothamBold
button.BorderSizePixel = 0
button.Parent = screenGui

local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(1, 0)
corner.Parent = button

local stroke = Instance.new("UIStroke")
stroke.Thickness = 3
stroke.Color = Color3.new(1, 1, 1)
stroke.Parent = button

-- ==================== ESTADO DO BOTÃO ====================
local isOn = false

local function toggle()
    isOn = not isOn
    if isOn then
        button.BackgroundColor3 = Color3.fromRGB(0, 180, 0)
        button.Text = "ON"
        print("✅ Sistema LIGADO")
    else
        button.BackgroundColor3 = Color3.fromRGB(220, 0, 0)
        button.Text = "OFF"
        print("⛔ Sistema DESLIGADO")
    end
end

button.MouseButton1Click:Connect(toggle)

-- ==================== ARRASTAR O BOTÃO ====================
local dragging = false
local dragStart, startPos

button.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        startPos = button.Position
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
        local delta = input.Position - dragStart
        button.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)

button.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = false
    end
end)

-- Visual Group (ESP)
local EspGroup = VisualTab:AddLeftGroupbox("Ore ESP")

EspGroup:AddToggle("Names", {Text = "Mostrar Nomes", Default = false})
    :OnChanged(function(v)
        EspLibrary.ItemNames.Enabled = v          -- Interage diretamente com a EspLibrary
    end)

EspGroup:AddToggle("Distance", {Text = "Mostrar Distância", Default = false})
    :OnChanged(function(v)
        EspLibrary.ItemDistances.Enabled = v       -- Interage diretamente com a EspLibrary
    end)

EspGroup:AddSlider("RenderDistance", {Text = "Render Distance", Default = 200, Min = 50, Max = 500})
    :OnChanged(function(v)
        EspLibrary.GeneralSettings.ItemRenderDistance = v   -- Interage com EspLibrary
    end)

-- Unload
MainTab:AddRightGroupbox("Controles"):AddButton({Text = "Unload Script", Func = function()
    Library:Unload()
    EspLibraryFunctions:Unload()   -- Remove todo o ESP
end})

Library:SetWatermark("UMT Completo Mobile")

-- ==================== 5. AUTO MINE + ESP (Loop principal) ====================
RunService.Heartbeat:Connect(function()
    Tool = GetTool()
    character = player.Character

    if not character or not character:FindFirstChild("HumanoidRootPart") then return end

    local rootPos = character.HumanoidRootPart.Position

    -- AUTO MINE
    if Settings.Farming.AutoMine and Tool then
        for _, ore in pairs(Workspace.SpawnedBlocks:GetChildren()) do
            if ore:IsA("MeshPart") and ore:GetAttribute("MineId") then
                local dist = (ore:GetPivot().Position - rootPos).Magnitude

                if dist <= Settings.Farming.AutoMineRange and dist > 8 then
                    -- Cria ESP usando a biblioteca (interação principal)
                    if not ore:FindFirstChild("ItemEsp") then
                        local data = {Name = ore:GetAttribute("MineId"), Type = "Ore"}
                        EspLibraryFunctions:CreateItemEsp(ore, data)
                    end

                    -- Executa mineração
                    local args = {1, ore:GetPivot().Position - Vector3.new(4,4,4)}
                    pcall(function()
                        ReplicatedStorage.MadCommEvents[Tool:GetAttribute("MadCommId")].Activate:FireServer(unpack(args))
                    end)
                end
            end
        end
    end
end)

-- ==================== 6. AUTO SELL + OUTROS LOOPS ====================
task.spawn(function()
    while true do task.wait(0.3)
        PlayersBackpack = character:WaitForChild("OrePackCargo", 5)
        Tool = GetTool()

        if Settings.Farming.AutoSell and not Selling then
            if character.OrePackCargo:GetAttribute("NumContents") == PlayersBackpack:GetAttribute("Capacity") then
                Selling = true
                OldPlayerPosition = character:GetPivot()
                character:PivotTo(PlayersUnloader:GetPivot() + Vector3.new(0,3,0))
                task.wait(0.2)
                fireproximityprompt(PlayersUnloader.Unloader.CargoVolume.CargoPrompt)
                task.wait(0.1)
                character:PivotTo(OldPlayerPosition)
                Selling = false
            end
        end
    end
end)

-- ==================== 7. EVENTOS DO JOGO (SpawnedBlocks e Character) ====================
Workspace.SpawnedBlocks.ChildAdded:Connect(function(ore)
    if ore:IsA("MeshPart") and ore:GetAttribute("MineId") then
        -- Cria ESP automaticamente quando novo ore aparece
        EspLibraryFunctions:CreateItemEsp(ore, {Name = ore:GetAttribute("MineId"), Type = "Ore"})
        
        if Settings.Farming.OreHitboxes then
            ore.Size = Vector3.new(Settings.Farming.OreHitboxesSize, Settings.Farming.OreHitboxesSize, Settings.Farming.OreHitboxesSize)
        end
    end
end)

player.CharacterAdded:Connect(function(newChar)
    character = newChar
    character:WaitForChild("Humanoid").WalkSpeed = Settings.Player.Walkspeed
end

-- ==================== TOGGLES E SLIDERS ====================

-- Auto Mine
FarmGroup:AddToggle("AutoMine", {Text = "Auto Mine", Default = false})
    :OnChanged(function(v)
        Settings.Farming.AutoMine = v
        print("Auto Mine:", v and "LIGADO" or "DESLIGADO")
    end)

-- Auto Sell
FarmGroup:AddToggle("AutoSell", {Text = "Auto Sell", Default = false})
    :OnChanged(function(v)
        Settings.Farming.AutoSell = v
        print("Auto Sell:", v and "LIGADO" or "DESLIGADO")
    end)

-- Mining Range
FarmGroup:AddSlider("MiningRange", {Text = "Mining Range", Default = 150, Min = 50, Max = 400, Rounding = 0})
    :OnChanged(function(v)
        Settings.Farming.AutoMineRange = v
        print("Mining Range atualizado para:", v)
    end)

-- Ore Hitbox
FarmGroup:AddToggle("OreHitbox", {Text = "Ore Hitbox", Default = false})
    :OnChanged(function(v)
        Settings.Farming.OreHitboxes = v
        
        -- Aplica o tamanho da hitbox em todos os ores existentes
        for _, ore in pairs(Workspace.SpawnedBlocks:GetChildren()) do
            if ore:IsA("MeshPart") then
                ore.Size = v and Vector3.new(Settings.Farming.OreHitboxesSize, Settings.Farming.OreHitboxesSize, Settings.Farming.OreHitboxesSize)
                         or Vector3.new(4.4, 4.4, 4.4)
            end
        end
        
        print("Ore Hitbox:", v and "LIGADO (Tamanho: " .. Settings.Farming.OreHitboxesSize .. ")" or "DESLIGADO")
    end)

-- Slider para mudar o tamanho da hitbox
FarmGroup:AddSlider("HitboxSize", {Text = "Hitbox Size", Default = 8, Min = 5, Max = 30, Rounding = 0})
    :OnChanged(function(v)
        Settings.Farming.OreHitboxesSize = v
        
        -- Se o hitbox estiver ativado, atualiza o tamanho imediatamente
        if Settings.Farming.OreHitboxes then
            for _, ore in pairs(Workspace.SpawnedBlocks:GetChildren()) do
                if ore:IsA("MeshPart") then
                    ore.Size = Vector3.new(v, v, v)
                end
            end
        end
        print("Hitbox Size atualizado para:", v)
    end)

-- ==================== LOOP PRINCIPAL - AUTO MINE ====================
RunService.Heartbeat:Connect(function()
    Tool = GetTool()
    character = player.Character
    
    if not character or not character:FindFirstChild("HumanoidRootPart") then return end
    
    if Settings.Farming.AutoMine and Tool then
        local rootPos = character.HumanoidRootPart.Position
        
        for _, ore in pairs(Workspace.SpawnedBlocks:GetChildren()) do
            if ore:IsA("MeshPart") and ore:GetAttribute("MineId") then
                local distance = (ore:GetPivot().Position - rootPos).Magnitude
                
                if distance <= Settings.Farming.AutoMineRange and distance > 8 then
                    -- Executa a mineração
                    local args = {1, ore:GetPivot().Position - Vector3.new(4, 4, 4)}
                    pcall(function()
                        ReplicatedStorage.MadCommEvents[Tool:GetAttribute("MadCommId")].Activate:FireServer(unpack(args))
                    end)
                end
            end
        end
    end
end)

-- Atualiza hitbox quando novos ores aparecem
Workspace.SpawnedBlocks.ChildAdded:Connect(function(ore)
    if Settings.Farming.OreHitboxes and ore:IsA("MeshPart") then
        ore.Size = Vector3.new(Settings.Farming.OreHitboxesSize, Settings.Farming.OreHitboxesSize, Settings.Farming.OreHitboxesSize)
    end
end)

-- Insta Mine Button (mantido do original)
MainTab:AddRightGroupbox("Exploits"):AddButton({Text = "Insta Mine", Func = function()
    for _, v in pairs(getgc(true)) do
        if type(v) == "table" and rawget(v, "Hardness") and rawget(v, "Speed") then
            rawset(v, "Speed", 2.5)
        end
    end
    Library:Notify({Title = "Insta Mine", Text = "Aplicado!", Duration = 3})
end})

-- ==================== FINALIZAÇÃO ====================
Library:SetWatermark("UMT - Farming System")

print("✅ Interface simples mobile carregada!")
print("   • Toque no botão circular para ligar/desligar")
print("   • Segure e arraste para mover o botão")
